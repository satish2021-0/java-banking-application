
package domain;

///Definition of enum:- It is a list of fix constant and it is used to restrict values at compile time so that invalid data never enters the system.
public enum Type {
    DEPOSIT,
    WITHDRAW,
    TRANSFER_IN,
    TRANSFER_OUT
}
